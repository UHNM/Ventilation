@using Ventilation.Components.Shared.LoanComponents
<Modal @ref="modal"
       UseStaticBackdrop="true"
       CloseOnEscape="false"
       Size="ModalSize.ExtraLarge">

    <BodyTemplate>

    </BodyTemplate>

    <FooterTemplate>
        <Button Color="ButtonColor.Secondary" @onclick="OnHideModalClick">Close</Button>

    </FooterTemplate>

</Modal>

<span class="pull-right"> <AddLoanModal PatientId="@(Patient.Id)"></AddLoanModal></span>

<Button Color="ButtonColor.Success" @onclick="OnAddLoanClick" Size="ButtonSize.Small"><Icon Name="IconName.PlusSquare" /></Button>

<div>

    @foreach (var loan in patientLoans)
    {
        <PatientLoanItem Patient="Patient" loan="loan" ></PatientLoanItem>
    }

</div>


